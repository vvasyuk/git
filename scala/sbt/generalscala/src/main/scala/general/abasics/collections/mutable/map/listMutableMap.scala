package general.abasics.collections.mutable.map

import scala.collection.mutable.ListMap

// A simple mutable map backed by a list, so it preserves insertion order.
// Use an immutable.ListMap assigned to a var instead of mutable.ListMap", "2.13.0
object listMutableMap {
  def main(args: Array[String]): Unit = {

  }
}
